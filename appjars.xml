<?xml version="1.0"?>

<!-- ===================================================================
     Fetch or define extra jars for this application
     =================================================================== -->
<project name="extraJars" basedir="." default="">
  <macrodef name="getExtraJars">
    <sequential>
      <delete dir="${org.bedework.temp.extrajars.dir}" />
      <mkdir dir="${org.bedework.temp.extrajars.dir}" />
    	
    	<property name="bedework.home"
    	          location="${project.home}/../bedework" />

    	<!-- Generated by this project -->
    	
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="bw-synch-common" version="${org.bedework.synch.version}"
              projecthome="${project.home}" />
      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="bw-synch" version="${org.bedework.synch.version}"
              projecthome="${project.home}" />

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="bw-synchsvc" version="${org.bedework.synch.version}"
              projecthome="${project.home}" />

      <!-- ======================= Schema jars ============================= -->

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="bw-exchangewsschema" version="${org.bedework.bwxml.version}"
              projecthome="${bedework.home}/../bwxml" />

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="bw-synchwsschema" version="${org.bedework.bwxml.version}"
              projecthome="${bedework.home}/../bwxml" />

      <!-- ====================== Bedework jars ============================ -->

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="rpiaccess" 
      	      version="${org.bedework.access.version}" project="access"
              projecthome="${bedework.home}/../access" />

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="rpiutil" version="${org.bedework.rpiutil.version}" project="rpiutil"
              projecthome="${bedework.home}/../rpiutil" />

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="bw-davio" version="${org.bedework.davutil.version}"
              projecthome="${bedework.home}/../davutil" />

      <!-- ======================= Commons jars ============================ -->
      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="commons-lang" version="2.1"/>
      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="commons-codec" version="1.5"/>

      <!-- ======================= httpclient jars ============================ -->
      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="httpcore" version="4.2.2"/>

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="httpclient" version="4.2.1"/>
      
      <!-- ======================= Ical4j jars ============================ -->

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="ical4j" version="head-20100601"/>

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="backport-util-concurrent" />

      <!--
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="commons-logging" />
              -->

      <!-- ======================= Hibernate jars ========================== -->
      
      <!--
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="antlr" version="2.7.6" />
              -->

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="asm" version="3.2" />
                      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="asm-attrs" />
      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="cglib" version="2.1.3" />
      
      <!--
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="dom4j" version="1.6.1" />
              -->
      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="ehcache" version="1.2.3" />
      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="hibernate" version="3.6.9.Final" />
      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="hibernate-validator" version="3.1.0.GA" />
      
      <!-- Other jars

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="commons-ssl" />

    	<getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="jgroups" version="2.2.8" />
    	
    	<getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="jta" version="1.1"  />
    	
    	<getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="commons-collections"/>

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="exchangews" version="2010"/>
 -->

      <!-- ======================= Geronimo jars ========================== -->

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="geronimo-kernel" version="2.2.1" />

      <!-- ========================= Spring jars ========================== -->

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="spring-beans" version="3.0.5.RELEASE" />

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="spring-context" version="3.0.5.RELEASE" />

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="spring-core" version="3.0.5.RELEASE" />

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="spring-expression" version="3.0.5.RELEASE" />
    </sequential>
 </macrodef>
</project>
